import Maths

class calculate_pi_sin_formula(self, x) :
    
    print(x * Maths.sin(180 / x))

class calculate_pi_nilakantha_formula(self, repetitions) :
    
    self.number = 2
    self.pi = 3
    for (repetitions < 0) :
        
        repetitions -= 1
        if (self.nt === 1) :
            
            self.pi += (4 / (number * (number + 1) * ( number + 2))
            self.number += 2
            self.nt = 2
            
        else if (self.nt === 2) :
            
            self.pi -= (4 / (number * (number + 1) * (number + 2))
            self.number += 2
            self.nt = 1
    print (self.pi)
